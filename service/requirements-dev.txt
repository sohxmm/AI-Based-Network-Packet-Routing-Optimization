# Development and test dependencies.
#
# pytest was in no requirements file at all, there was no pytest.ini and no
# conftest.py, so on a clean clone the suite could not be run. "11 test files"
# is not a claim that survives verification if nothing can execute them.
-r requirements.txt
pytest==8.2.0
pytest-asyncio==0.23.7
pytest-cov==5.0.0
httpx==0.27.0
ruff==0.4.4
tensorboard>=2.16.0
