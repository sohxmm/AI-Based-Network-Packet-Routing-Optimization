# Runtime dependencies.
#
# Removed after confirming zero imports anywhere in the codebase:
#   scikit-learn, pandas, python-dotenv  (~200 MB of image for nothing)
#   websockets                           (already provided by uvicorn[standard];
#                                         pinning it separately invites conflicts)
# Moved to requirements-dev.txt: httpx (only fastapi.TestClient needs it).
fastapi==0.111.0
uvicorn[standard]==0.29.0
networkx==3.3
numpy==1.26.4
torch==2.3.0
stable-baselines3==2.3.2
gymnasium==0.29.1
sqlalchemy==2.0.30
alembic==1.13.1
asyncpg==0.29.0
pydantic==2.7.1
scipy>=1.13.0
matplotlib>=3.9.0
