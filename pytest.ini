[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
addopts = -q --strict-markers
markers =
    slow: long-running stress tests (deselect with -m "not slow")
    requires_model: needs a trained model artifact to be present
    requires_db: needs a reachable PostgreSQL instance
